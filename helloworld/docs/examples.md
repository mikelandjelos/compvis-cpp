/*!
\defgroup examples Examples

Concrete example programs demonstrating OpenCV techniques.
Each example is implemented as a function with signature `int(int,char**)` and
registered via the `REGISTER_EXAMPLE` macro.

See the runner CLI in `main.cpp` for how to list/run examples.
*/

